.DS_Store
/.build
/.build-seq
/Packages
xcuserdata/
DerivedData/
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc
# code-monkey indexes, wherever they land — the CLI writes one relative to wherever
# it was invoked, so a stray can appear outside the project root's .code-monkey/.
.code-monkey/
index.db
index.db-shm
index.db-wal
