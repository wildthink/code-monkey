{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.swift) \"$CLAUDE_PROJECT_DIR/hooks/_reindex.sh\" ;; esac; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Refreshing code-monkey index"
          }
        ]
      }
    ]
  }
}
